node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Log files - don't commit
logs/

# Large binaries - don't commit
claude.exe

# Environment variables - NEVER COMMIT
.env
.env.local
.env.*.local

# Claude settings with API keys - NEVER COMMIT
.claude/settings.local.json