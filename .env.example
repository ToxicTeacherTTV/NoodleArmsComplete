# 🤖 GEMINI API CONFIGURATION
GEMINI_API_KEY=your_api_key_here

# 🎯 TIERED MODEL SELECTION (Optional - defaults provided)
# 
# COST COMPARISON (per 1M input tokens):
# - gemini-2.5-pro:           $1.25 (premium quality, expensive)
# - gemini-2.5-flash:         $0.075 (17x cheaper, good quality) 
# - gemini-2.0-flash-exp:     Free tier (experimental, may be unstable)
# - gemini-2.0-flash-thinking-exp-01-21: Free tier (improved reasoning, experimental)
#
# FLASH MODEL HISTORY:
# Flash models were previously banned due to hallucination issues (269 false memories).
# After investigation, Flash Thinking models show improved reliability.
# This config allows controlled use with automatic fallback safety.

# Development model (used when NODE_ENV=development)
# Default: gemini-2.0-flash-exp (free tier for testing)
# GEMINI_DEV_MODEL=gemini-2.0-flash-exp

# Production default model (used for most operations)
# Default: gemini-2.5-flash (cost-effective, reliable)
# GEMINI_DEFAULT_MODEL=gemini-2.5-flash

# Fallback model (used if primary fails)
# Default: Automatically chosen based on primary model
# GEMINI_FALLBACK_MODEL=gemini-2.5-pro

# Extraction model (used for fact/entity extraction - accuracy critical)
# Default: gemini-2.5-flash (production-ready only, no experimental)
# GEMINI_EXTRACTION_MODEL=gemini-2.5-flash

# Analysis model (used for content analysis/flagging - premium quality)
# Default: gemini-2.5-pro (best quality for critical analysis)
# GEMINI_ANALYSIS_MODEL=gemini-2.5-pro

# 💡 RECOMMENDATIONS:
#
# For maximum cost savings during development:
# - Set GEMINI_DEV_MODEL=gemini-2.0-flash-exp (free)
# - Set GEMINI_DEFAULT_MODEL=gemini-2.5-flash (cheap)
# - Leave GEMINI_ANALYSIS_MODEL=gemini-2.5-pro (quality when it matters)
#
# For production with quality focus:
# - Set GEMINI_DEFAULT_MODEL=gemini-2.5-pro (best quality)
# - Set GEMINI_FALLBACK_MODEL=gemini-2.5-flash (backup)
#
# For production with cost focus (recommended):
# - Set GEMINI_DEFAULT_MODEL=gemini-2.5-flash (cost-effective)
# - Set GEMINI_FALLBACK_MODEL=gemini-2.5-pro (quality backup)
# - Set GEMINI_ANALYSIS_MODEL=gemini-2.5-pro (premium for critical tasks)

# Other API keys (existing configuration)
# ANTHROPIC_API_KEY=your_anthropic_key
# OPENAI_API_KEY=your_openai_key
# DATABASE_URL=your_database_url
